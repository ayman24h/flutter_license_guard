name: license_guard_example
description: Example Flutter Desktop app using flutter_license_guard.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_license_guard:
    path: ../packages/flutter_license_guard

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
